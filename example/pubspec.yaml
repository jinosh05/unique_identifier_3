name: unique_identifier_3_example
description: "Demonstrates how to use the unique_identifier_3 plugin."
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  unique_identifier_3:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
